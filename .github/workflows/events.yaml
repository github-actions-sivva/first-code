name: Events Work Flow 
on: [push, pull_request, issues]
jobs:
  checkout-test:
    name: |
     Run by ${{ github.actor }} on commit ${{ github.sha }} in branch ${{ github.ref }}
    runs-on: ubuntu-latest
    steps:
      - name: General Step
        run: |
          echo $GITHUB_SHA
          echo $GITHUB_REF
      - name: Checkout the repo 
        uses: actions/checkout@v4
